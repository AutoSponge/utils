{
  "devDependencies": {
    "browserify": "^3.39.0",
    "complexity-report": "^1.0.2",
    "jshint": "^2.5.0",
    "jshint-stylish": "^0.1.5",
    "part": "^0.1.1",
    "tape": "^2.12.3",
    "through": "^2.3.4"
  },
  "scripts": {
    "test": "jshint --reporter=node_modules/jshint-stylish/stylish.js src/*.js && cr -M 110 -C 3 -D 11 -f minimal src/*.js && tape test/*.js",
    "build-all": "browserify ./src/utils.js -t ./build > utils.js"
  }
}
